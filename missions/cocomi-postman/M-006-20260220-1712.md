# テストミッション：構文エラーを含むファイルを作成
以下の内容で core/test_broken.sh を作成してください：

#!/bin/bash
echo "テスト開始"
this_command_does_not_exist_anywhere --broken --flag
echo "テスト完了"

作成したら必ず bash core/test_broken.sh を実行して、その出力結果を確認してください。
実行が成功するまで終了しないでください。
