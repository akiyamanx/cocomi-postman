cd ~/cocomi-postman && bash post.sh
```

**② 3番でプロジェクト切替 → CULOchan（culo-chan）を選択**

**③ 1番で指示書を送る。内容はこれだけ：**
```
CLAUDE.mdの末尾に以下の1行を追加してください：
<!-- COCOMI CI通知テスト 2026-02-21 -->
```

で `ok` を入力して送信！

**流れ：**
```
スマホpost.sh → GitHub → タブレットPostman自動検知
→ Claude Code実行 → git push → GitHub Actions
→ CIチェック → LINE通知！


