---

## Session07: 2026-03-01（午後〜夜）Phase2 OpenCV.js実装＆実機動作確認
**DIFF:** `開発カプセル_DIFF_DEV_culo-chan_2026-03-01_07` / `思い出カプセル_DIFF_総合_2026-03-01_07`

### やったこと
- Phase2 OpenCV.js導入: receipt-multi-crop.js v2.0→v2.3（4回イテレーション）
  - v2.0: Cannyエッジ → 背景テクスチャが結合して失敗
  - v2.1: 白紙閾値180 → 成功だが回転方向が逆
  - v2.2: 正面化→左90度回転 → Python検証完璧、ブラウザでOpenCV.js未読込み発覚
  - v2.3: 閾値170+5%マージン → 端切れ改善
- OpenCV.js CDN問題解決: docs.opencv.org → jsdelivr CDN（1秒読み込み）
- Service Workerキャッシュ問題: innerHTML内のscriptが実行されない → JSファイルからDOM更新に変更
- デバッグバー（ocvDebugBar）追加で状態可視化
- 黒背景テスト: 3枚のレシートを個別に縦向きで切り出し成功！

### バージョン変更
- receipt-multi-crop.js: 345行(v1.0 Canvas版) → 430行(v2.3 OpenCV版)
- index.html: OpenCV.js CDN変更（jsdelivr優先+フォールバック）
- receipt-viewer.html: デバッグ表示div追加
- sw.js: v2.14.0 → v2.21.0（7バージョン更新）

### 重要な設計判断
1. **Cannyエッジ vs 白紙閾値**: Cannyは背景テクスチャに弱い → 白紙閾値(170)が安定
2. **回転補正**: dst_ptsで回転させるより、正面化→cv.rotate()の2段階が確実
3. **CDN選択**: docs.opencv.orgはスマホで不安定 → jsdelivrが1秒で安定
4. **デバッグ方式**: HTMLフラグメント内のscriptは実行されない → JSからDOM更新

### 重要な発見
- **背景色が検出精度に大きく影響**: カーペット(灰色)→黒い布で劇的改善
- **OpenCV.jsは8MBだがjsdelivr CDNなら1秒で読み込める**
- **Service Workerのキャッシュは開発時に大きな障害になる**
- **Python検証→JavaScript実装の2段階プロセスが効率的**
