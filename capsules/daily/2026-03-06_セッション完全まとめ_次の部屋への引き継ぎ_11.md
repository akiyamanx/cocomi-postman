# 2026-03-06 セッション完全まとめ — 次の部屋への引き継ぎ_11

## 1行サマリー
**SAM2-tiny ONNXがGalaxy S22 Ultraで完全動作！レシート1枚ずつタップ→切り抜き成功。base_plus(340MB)は読込失敗→tiny一択確定。CULOchan本体コード変更なし。**

## ★最重要★ Session11の成果

### SAM2-tiny Galaxy S22 Ultra 動作実証データ
| 項目 | 数値 |
|------|------|
| エンコーダー読込（初回） | 14.2秒 |
| エンコーダー読込（キャッシュ後） | 1.1〜2.1秒 |
| デコーダー読込 | 0.3〜0.5秒 |
| エンコーダー推論 | 21〜28秒 |
| **デコーダー推論** | **213〜334ms** |
| レシート検出スコア | 最大97.9% |
| マスク出力サイズ | 256×256 |
| base_plus(340MB) | ❌ 読込失敗 |

### 作成したデモファイル
- `sam2-receipt-demo.html` — SAM2-tiny版（✅動作確認済み、これを使う）
- `sam2-receipt-demo-baseplus.html` — base_plus版（❌参考用）
- `culochankaikei-category-mock.jsx` — カテゴリカードUIモック
- `sam-segmentation-demo.jsx` — セグメンテーション説明デモ

## ★最優先★ 次にやること

### 1. receipt.htmlのゴミ箱はみ出し修正（Session10からの持ち越し！）
```bash
grep -n "🗑\|ゴミ\|trash\|gallery" ~/CULOchanKAIKEIpro/receipt.html | head -10
```
→ ボタン行のHTML/CSSを確認して修正

### 2. AI個別解析の動作確認（Session10からの持ち越し）
- 枠指定 → 確定 → 「AIで読み取る」
- 進捗表示「AI解析中... (1/8枚)」が出るか確認
- 解析結果が入力フォームに反映されるか確認

### 3. SAM2統合: receipt-sam.js 設計・実装
```
新規ファイル: receipt-sam.js
  ├ モデル読込（スプラッシュ裏・非同期）
  ├ Service Workerキャッシュ管理
  ├ エンコーダー推論（画像選択後）
  ├ デコーダー推論（タップごと）
  ├ マスク表示（Canvasオーバーレイ）
  ├ 切り抜き→multiImageDataUrlsに格納
  └ フォールバック（未ロード→手動枠指定）
```

### 4. その後の優先順
①合計欄横線バグ ②Excel出力マス目付き書式 ③印鑑レイヤー ④A4縦テンプレ調整 ⑤マスター候補ドロップダウンバグ

## 技術背景（次のクロちゃんが知るべきこと）

### SAM2のアーキテクチャ
```
エンコーダー（134MB、ORT形式）
  入力: [1, 3, 1024, 1024] float32（RGB正規化: (px/255)*2-1）
  出力: image_embed, high_res_feats_0 [1,32,256,256], high_res_feats_1 [1,64,128,128]
  推論: 21〜28秒（Galaxy S22, WASM）

デコーダー（20.6MB、ONNX形式）
  入力: image_embed, high_res_feats_0/1, point_coords, point_labels, mask_input, has_mask_input
  ★★★ orig_im_sizeは不要！（SAM1との違い）★★★
  出力: masks [1, 3, 256, 256], iou_predictions [1, 3]
  推論: 213〜334ms
```

### SAM2モデルURL
```javascript
// ✅ 使うのはこれ（Labelbox公開、ORT最適化済み）
const ENCODER_URL = 'https://storage.googleapis.com/lb-artifacts-testing-public/sam2/sam2_hiera_tiny.encoder.ort';
const DECODER_URL = 'https://storage.googleapis.com/lb-artifacts-testing-public/sam2/sam2_hiera_tiny.decoder.onnx';
```

### マスク座標変換（超重要！）
```
マスクは256×256で出力される（1024×1024ではない！）

タップ座標の変換:
  元画像座標 × scale + offset = 1024座標 → デコーダーに渡す

マスクの変換:
  マスク座標(256) × (1024/256) = 1024座標
  1024座標 - offset = スケール画像座標
  スケール画像座標 / scale = 元画像座標

オーバーレイ描画:
  maskScale = 256 / 1024 = 0.25
  srcX = offsetX × maskScale
  srcY = offsetY × maskScale
  srcW = imgWidth × scale × maskScale
  srcH = imgHeight × scale × maskScale
```

### ONNX Runtime Web
```html
<script src="https://cdn.jsdelivr.net/npm/onnxruntime-web@1.20.1/dist/ort.min.js"></script>
```
- executionProviders: `['wasm']`（最も互換性が高い）

### 画像前処理
```javascript
// アスペクト比維持、1024×1024にリサイズ、黒パディング
const scale = Math.min(1024 / img.width, 1024 / img.height);
const offsetX = (1024 - img.width * scale) / 2;
const offsetY = (1024 - img.height * scale) / 2;
// 正規化: (pixel / 255.0) * 2 - 1
```

### SAM2統合時のCULOchanとの接続ポイント
```
receipt-sam.js の切り抜き結果
  ↓ multiImageDataUrls = [切り抜き画像1, 切り抜き画像2, ...]
  ↓ renderMultiImageThumbnails()
→ 「AIで読み取る」ボタンが有効化
  ↓ receipt-ai-patch.js の runAiOcr()
  ↓ 1枚ずつ analyzeReceiptWithGemini()
→ 既存フローに合流（変更不要）
```

### Session11で発見した重要な事実
1. **SAM2はorig_im_sizeが不要**（SAM1との最大の違い）
2. **マスク出力は256×256**（1024×1024ではない）
3. **base_plus(340MB)はGalaxy S22で動かない**（tiny一択）
4. **折り目のあるレシートも統合検出できる**（Gemini/OpenCVでは不可能だった）
5. **キャッシュ後のモデル読込は2秒以内**（スプラッシュ裏読みで十分）
6. **デコーダー推論300msはタップ即反応**（UIとして十分な速度）

## git log（Session11ではpushなし）
CULOchanKAIKEIpro本体へのコード変更なし。
SAM2デモHTMLはclaude.aiからダウンロードしてローカルテスト。

## ファイル構成（Session10終了時点のまま）
```
receipt-frame-modal.js  (475行) v1.2: 手動枠指定モーダル
receipt-ai-patch.js     (68行)  v1.0: runAiOcr()上書き個別解析
receipt-ai.js           (309行) v6.0: Gemini AIフル解析（変更なし）
receipt-core.js                 変更なし
receipt-purpose.js              変更なし
index.html                      226行目＋218行目にscript追加
receipt.html                    48行目に枠指定ボタン追加
```

## 開発環境メモ
- Termux TMPDIR: `export TMPDIR=~/tmp && mkdir -p ~/tmp`
- GitHub Pages反映: push後5〜10分待ち＋キャッシュクリア必須
- キャッシュクリア: chrome://serviceworker-internals でUnregister→2回リロード
- GitHubユーザー名: akiyamanx
- **cpコマンド後は必ずls -laでサイズ確認**

## claude.ai活用メモ（Session11で確立）
- **アーティファクト**: React/HTMLのUIモック・プロトタイプ（その場でプレビュー）
- **デモHTML作成→ダウンロード→実機テスト**: の流れがSAM検証で有効だった
- **本番コード変更はTermux＋Claude Code**: 複数ファイル連携・git操作が必要
- **企画・相談・調査**: claude.aiが得意。Web検索もできる

---
*作成: クロちゃん / 2026-03-06 Session11*
